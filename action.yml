name: Github to AWS CodeCommit Sync
description: 'Synchronize from GitHub to CodeCommit via GitHub Actions using IAM credentials.'
author: 'tsgit18'
branding:
  icon: terminal
  color: yellow
inputs:
  repository_name:
    description: CodeCommit repository name.
    required: true
  aws_region:
    description: Region of the CodeCommit repository.
    required: true
  codecommit_url:
    description: CodeCommit Url including HTTPS Git credentials for AWS CodeCommit.
    required: true
  branch_from:
    description: From branch.
    required: true
  branch_to:
    description: To branch.
    required: true
runs:
  using: 'docker'
  image: './Dockerfile'
